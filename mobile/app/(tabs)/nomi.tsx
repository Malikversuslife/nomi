import { ScreenShell } from "@/components/ScreenShell";

export default function NomiScreen() {
  return (
    <ScreenShell
      eyebrow="Ask Nomi"
      title="Let's figure it out together."
      description="Ask for another explanation, an example, a hint, or a quick quiz without losing the context of what you're learning."
    />
  );
}
