import { ScreenShell } from "@/components/ScreenShell";

export default function LearnScreen() {
  return (
    <ScreenShell
      eyebrow="Learn"
      title="Build it one idea at a time."
      description="Explore subjects and concepts through a curriculum Nomi can adapt around your progress."
    />
  );
}
